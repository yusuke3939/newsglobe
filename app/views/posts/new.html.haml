.section__title
  %h1 /新規記事の作成/
.section__contents

  .form
    = form_for @post, local: true do |f|
      %p タイトル
      .form__top
        = f.text_field :title, class: :form__top__title
        = f.label :image, class: 'form__top__image' do
          = icon('fas', 'image', class: 'icon')
          = f.file_field :image, class: 'hidden'
      %p 国、地域
      .form__region
        -# = f.label 
        = f.text_field :region_list, value: @post.region_list.join(','), class: :form__region__input
      %p 本文
      .form__text
        = f.text_area :content, class: :form__content
        = f.submit "投稿する", class: :send__btn
      